[
  {
    "name": "load_markdown",
    "description": "Load an existing markdown artifact from the project. Use this tool when you need to read a previously saved markdown document.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "artifact_id": {
          "type": "string",
          "description": "The unique identifier of the markdown artifact to load"
        }
      },
      "required": ["artifact_id"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "artifact_id": {
          "type": "string",
          "description": "Unique identifier of the artifact"
        },
        "filename": {
          "type": "string",
          "description": "Filename of the markdown"
        },
        "content": {
          "type": "string",
          "description": "The markdown content"
        },
        "created_at": {
          "type": "string",
          "description": "ISO 8601 timestamp of when the artifact was created"
        }
      }
    }
  }
]
